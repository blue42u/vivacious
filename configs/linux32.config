# This variant cross-compiles for a 32-bit linux. All cross-compiling configs
# provided use Clang, so make sure its installed.

# We kind of assume the AR is still ar...
CONFIG_CC=clang
CONFIG_AR=ar

CONFIG_CFLAGS=--target=i386-linux-gnu
CONFIG_LDLIBS=-ldl
